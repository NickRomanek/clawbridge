# ClawBridge - Python Dependencies
# ==================================

# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0

# Async support
httpx>=0.28.0
websockets>=14.0

# Browser automation engine
browser-use>=0.11.0
playwright>=1.49.0

# LLM providers (BYOK)
langchain-anthropic>=0.3.0
langchain-openai>=0.3.0

# Configuration and environment
python-dotenv>=1.0.0

# Computer-use engine (desktop automation)
anthropic>=0.39.0
pyautogui>=0.9.54
Pillow>=10.0.0
mss>=9.0.0
pywinauto>=0.6.8; sys_platform == 'win32'

# macOS platform backend (PyObjC frameworks)
pyobjc-framework-Cocoa>=10.0; sys_platform == 'darwin'
pyobjc-framework-Quartz>=10.0; sys_platform == 'darwin'
pyobjc-framework-ApplicationServices>=10.0; sys_platform == 'darwin'

# Input recording for workflow capture
pynput>=1.7.6

# System tray icon
pystray>=0.19.0

# MCP server (optional, for Claude Code integration)
mcp>=1.26.0
